# TODO: Fill in notification preferences

language: ruby
cache: bundler

before_script:
  - bundle install
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start

script:
  - bundle exec cucumber

notifications:
  email:
    - insert_email
  slack: smashingboxes:some_number
